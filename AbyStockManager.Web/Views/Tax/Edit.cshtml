@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer ViewLocalizer

@model AbyStockManager.Web.Model.ViewModel.Tax.EditTaxViewModel
@{
    ViewData["Title"] = @ViewLocalizer["Edit"] + " " + @ViewLocalizer["Tax"];
}

@section script{
    <partial name="~/Views/Shared/_FormScriptsPartial.cshtml" />
}
<section class="content pt-2">
    <div class="container-fluid ">
        <div class="row ">
            <div class="col-md-6">
                <div class="card card-warning">
                    <div class="card-header">
                        <h3 class="card-title"><i class="fas fa-pen"></i> @ViewLocalizer["Edit"] + " " + @ViewLocalizer["Tax"]</h3>
                    </div>
                    <form asp-action="Edit" asp-antiforgery="true" id="frmCreate">
                        <div class="card-body">
                            <div class="form-group">
                                @ViewLocalizer["Name"]
                                <input asp-for="Name" type="text" class="form-control" placeholder="@ViewLocalizer["Enter"] @ViewLocalizer["Name"]">
                                <span asp-validation-for="Name" class="text-danger"></span>
                            </div>
                            <div class="form-group">
                                @ViewLocalizer["Rate"]
                                <input asp-for="Rate" type="number" class="form-control" step="any" min="0.1" placeholder="@ViewLocalizer["Enter"] @ViewLocalizer["Rate"]">
                                <span asp-validation-for="Rate" class="text-danger"></span>
                            </div>
                        </div>
                        <div class="card-footer text-right">
                            <a class="btn btn-outline-secondary" href="/Tax"><i class="fas fa-arrow-left"></i>  @ViewLocalizer["Back"]</a>
                            <button id="btnSave" type="submit" class="btn btn-warning"><i class="fas fa-pen"></i>  @ViewLocalizer["Edit"]</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>



