@model Aby.StockManager.Model.ViewModel.ServiceCategory.CreateServiceCategoryViewModel
@{
    ViewData["Title"] = "Service Type Create";
}
@section script{
    <partial name="~/Views/Shared/_FormScriptsPartial.cshtml" />
}
<section class="content pt-2">
    <div class="container-fluid ">
        <div class="row ">
            <div class="col-md-6">
                <div class="card card-success">
                    <div class="card-header">
                        <h3 class="card-title"><i class="fas fa-plus"></i> Create Service Type</h3>
                    </div>
                    <form asp-action="Create" asp-antiforgery="true" id="frmCreate">
                        <div class="card-body">
                            <div class="form-group">
                                <label asp-for="ServiceCategoryName"></label>
                                <input asp-for="ServiceCategoryName" type="text" class="form-control">
                                <span asp-validation-for="ServiceCategoryName" class="text-danger"></span>
                            </div>
                        </div>
                        <div class="card-footer text-right">
                            <a class="btn btn-outline-secondary" href="/ServiceCategory"><i class="fas fa-arrow-left"></i> Back</a>
                            <button id="btnSave" type="submit" class="btn btn-success"><i class="fas fa-plus"></i> Create</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>

